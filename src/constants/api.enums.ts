export enum ACCOUNT_TYPE {
  BACKEND = 'backend',
  FRONTEND = 'frontend',
}

export enum ROLES {
  USER = 'user',
  ADMIN = 'admin',
}

export enum PROJECT_TYPE {
  FRONTEND = 'frontend',
  BACKEND = 'backend',
}
